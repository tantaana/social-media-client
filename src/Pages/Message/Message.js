import React from 'react';
import AllTitle from '../../Hooks/AllTitle';

const Message = () => {
    AllTitle('Message')
    return (
        <div>
            <h2>message</h2>
        </div>
    );
};

export default Message;