import React from 'react';
import AllTitle from '../../Hooks/AllTitle';

const About = () => {
    AllTitle('About')
    return (
        <div>
            <h2>about</h2>
        </div>
    );
};

export default About;